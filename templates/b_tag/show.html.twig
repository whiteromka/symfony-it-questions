{% extends 'layouts/layout.html.twig' %}

{% block title %}BTag{% endblock %}

{% block body %}
    <h1>BTag</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ b_tag.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ b_tag.name }}</td>
            </tr>
        </tbody>
    </table>
    <a class="btn btn-outline-dark" href="{{ path('b_tag_index') }}">Назад в список</a>
    <a href="{{ path('b_tag_edit', {'id': b_tag.id}) }}">edit</a>
    {{ include('b_tag/_delete_form.html.twig') }}
{% endblock %}
