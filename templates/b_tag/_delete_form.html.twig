<form method="post" action="{{ path('b_tag_delete', {'id': b_tag.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ b_tag.id) }}">
    <button class="btn btn-danger">Удалить</button>
</form>
